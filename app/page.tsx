'use client';
import { useCallback, useEffect, useState, type FormEvent, type ReactNode } from 'react';
import Link from 'next/link';
import LiveCarePlans, {type StoredPlan,type PlanReview} from './components/LiveCarePlans';
import { Activity, LayoutDashboard, Users, Radio, ListChecks, FileSpreadsheet, Settings, LogOut, RefreshCw, ShieldCheck } from 'lucide-react';
import type { AutomaticReading } from '../lib/ingestion/reading';
import { csvCell } from '../lib/report';

type Person = {id:string;name:string;town:string;active:number;consent_at:string};
type Device = {id:string;label:string;person_id:string;adapter_id:string;kind:string;enabled:number};
type Integration = {id:string;name:string;enabled:number};
type User = {id:string;email:string;role:string;active?:number};
type Reading = {event_key:string;person_id:string;device_id:string;measured_at:string;received_at:string;payload:AutomaticReading};
type Task = {id:string;person_id:string;title:string;owner:string|null;stage:string};
type Snapshot = {plans:StoredPlan[];planReviews:PlanReview[];planTasks:{plan_id:string;version:number;task_id:string}[];user:User;people:Person[];devices:Device[];integrations:Integration[];readings:Reading[];tasks:Task[];users:User[];audit:{id:number;actor:string;action:string;subject:string;at:string}[]};
const navigation=[['Overview',LayoutDashboard],['People',Users],['Devices',Radio],['Tasks',ListChecks],['Care plans',ShieldCheck],['Reports',FileSpreadsheet],['Workspace',Settings]] as const;
type View=typeof navigation[number][0];
const date=(v:string)=>new Intl.DateTimeFormat('en-ZA',{dateStyle:'medium',timeStyle:'short',timeZone:'Africa/Johannesburg'}).format(new Date(v));
function value(r:Reading){const m=r.payload.measurement;return m.kind==='blood-pressure'?`${m.systolic}/${m.diastolic} ${m.unit}${m.pulse===undefined?'':` · Pulse ${m.pulse} bpm`}`:`${m.value} ${m.unit}${m.kind==='spo2'&&m.pulse!==undefined?` · Pulse ${m.pulse} bpm`:''}`;}
async function api(path:string,method='GET',data?:unknown){const res=await fetch(`/api/${path}/`,{method,cache:'no-store',headers:data?{'Content-Type':'application/json'}:undefined,body:data?JSON.stringify(data):undefined});const result=await res.json();if(!res.ok)throw new Error(result.error||'Request failed.');return result;}
function Input({name,label,type='text',required=true}:{name:string;label:string;type?:string;required?:boolean}){return <label className="field">{label}<input name={name} type={type} required={required} maxLength={type==='password'?256:160} minLength={type==='password'?14:undefined} autoComplete={type==='password'?'new-password':undefined}/></label>;}
function Form({children,label,onSubmit,busy}:{children:ReactNode;label:string;onSubmit:(data:Record<string,unknown>)=>Promise<boolean>;busy:boolean}){async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const fields=Object.fromEntries(new FormData(form));if(await onSubmit(fields))form.reset();}return <form className="live-form" onSubmit={submit}>{children}<button className="primary" disabled={busy}>{busy?'Saving…':label}</button></form>;}
export default function CareGrid(){
 const [auth,setAuth]=useState<{configured:boolean;user:User|null}|null>(null),[data,setData]=useState<Snapshot|null>(null),[view,setView]=useState<View>('Overview'),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[query,setQuery]=useState(''),[personId,setPersonId]=useState(''),[secret,setSecret]=useState<{secret:string;adapterId:string}|null>(null);
 const [recoveryLink,setRecoveryLink]=useState('');
 const refresh=useCallback(async()=>{const a=await api('auth');setAuth(a);if(a.user)setData(await api('workspace'));else {setData(null);setSecret(null);setRecoveryLink('');}},[]);
 useEffect(()=>{refresh().catch(e=>setMessage(e.message));const timer=setInterval(()=>{if(document.visibilityState==='visible')refresh().catch(()=>setMessage("Connection lost. Displayed records may be out of date. Use Refresh to retry."));},30000);return()=>clearInterval(timer);},[refresh]);
 useEffect(()=>{const sync=()=>{const found=navigation.find(([name])=>`#/${name.toLowerCase().replace(/ /g,'-')}`===location.hash);setView(found?.[0]||'Overview');};sync();window.addEventListener('hashchange',sync);return()=>window.removeEventListener('hashchange',sync);},[]);
 function go(next:View){setView(next);location.hash=`/${next.toLowerCase().replace(/ /g,'-')}`;setPersonId('');setMessage('');}
 async function mutate(payload:Record<string,unknown>){setBusy(true);setMessage('');try{const result=await api('workspace','POST',payload);if(result.secret)setSecret(result);if(result.recoveryToken)setRecoveryLink(`${location.origin}/recover/#${result.recoveryToken}`);await refresh();setMessage('Changes saved.');return true;}catch(e){setMessage((e as Error).message);return false;}finally{setBusy(false);}}
 const notice=<p role="status" className="live-notice">{message}</p>;
 if(!auth)return <main className="auth-page"><div className="panel"><Activity/><h1>Opening CareGrid</h1>{notice}<button className="secondary" onClick={()=>refresh().catch(e=>setMessage(e.message))}>Retry connection</button></div></main>;
 if(!auth.user||!data)return <main className="auth-page"><section className="panel auth-panel"><span className="brand"><Activity/> CareGrid</span><span className="eyebrow">NEXT-GEN HEALTHCARE AT HOME</span><h1>{auth.configured?'Welcome back.':'Your care workspace starts here.'}</h1><p>{auth.configured?'Sign in to your connected-care workspace.':'Create the first administrator account. Your workspace starts empty, with automatic readings only.'}</p>{notice}<Form busy={busy} label={auth.configured?'Sign in':'Create administrator'} onSubmit={async fields=>{setBusy(true);try{await api('auth','POST',{...fields,action:auth.configured?'login':'setup'});await refresh();setMessage('');return true;}catch(e){setMessage((e as Error).message);return false;}finally{setBusy(false);}}}><Input name="email" label="Email" type="email"/><Input name="password" label="Password · at least 14 characters" type="password"/>{!auth.configured&&<Input name="setupToken" label="Server setup token · not needed for enabled local setup" required={false} type="password"/>}</Form><p className="muted">{auth.configured?'Accounts are managed by your workspace administrator.':'Keep your administrator password in a password manager.'}</p><Link href="/demo/">Explore the separate fictional demo</Link></section></main>;
 const admin=data.user.role==='admin';
 const personName=(id:string)=>data.people.find(p=>p.id===id)?.name||id;
 const personOptions=<><option value="">Choose a person</option>{data.people.filter(p=>p.active).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</>;
 const personSelect=<label className="field">Person<select name="personId" required>{personOptions}</select></label>;
 const readings=personId?data.readings.filter(r=>r.person_id===personId):data.readings;
 function readingTable(rows:Reading[]){return rows.length?<div className="table-scroll"><table><thead><tr><th>Person</th><th>Measurement</th><th>Reading</th><th>Measured · SAST</th><th>Source device</th></tr></thead><tbody>{rows.map(r=><tr key={r.event_key}><td>{personName(r.person_id)}</td><td>{r.payload.measurement.kind}</td><td>{value(r)}</td><td>{date(r.measured_at)}</td><td>{r.device_id}</td></tr>)}</tbody></table></div>:<div className="empty"><Radio/><h3>No automatic readings yet</h3><p>Enrol a device and configure its gateway or cloud connector to send readings.</p></div>;}
 function download(){const rows=[['Person','Device','Measurement','Value','Measured at','Received at','Source event'],...readings.map(r=>[personName(r.person_id),r.device_id,r.payload.measurement.kind,value(r),r.measured_at,r.received_at,r.payload.sourceEventId])];const url=URL.createObjectURL(new Blob(['\uFEFF'+rows.map(row=>row.map(csvCell).join(',')).join('\r\n')],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='caregrid-readings.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
 return <div className="shell"><a className="skip" href="#main">Skip to workspace</a><aside className="sidebar"><Link href="/" className="brand"><span className="brand-mark"><Activity/></span>Care<span>Grid</span></Link><p className="workspace-label">HOMECLINICSTORE / CARE TEAM</p><nav aria-label="Main navigation">{navigation.map(([name,Icon])=><button key={name} aria-current={view===name?'page':undefined} onClick={()=>go(name)}><Icon size={19}/><span>{name}</span></button>)}</nav><div className="sidebar-note"><ShieldCheck/><strong>Connected devices. Clear context.</strong><p>Automatic readings, with the people behind them.</p></div><div className="team"><div><strong>{data.user.email}</strong><small>{data.user.role}</small><button className="text-button" onClick={async()=>{try{await api('auth','DELETE');setSecret(null);await refresh();}catch(e){setMessage((e as Error).message);}}}><LogOut size={15}/> Sign out</button></div></div></aside><div className="workspace"><header className="topbar"><span>CareGrid / {view}</span><button className="text-button" onClick={()=>refresh().then(()=>setMessage('Workspace refreshed.')).catch(e=>setMessage(e.message))}><RefreshCw size={15}/> Refresh</button></header><main id="main"><div className="page-heading"><div><span className="eyebrow">CONNECTED CARE AT HOME</span><h1>{view==='Overview'?'A clearer view of care.':view==='People'?'People, not just readings.':view==='Devices'?'The connections behind care.':view==='Tasks'?'Follow-through, made visible.':view==='Reports'?'A shared view of progress.':view==='Care plans'?'Plans with a clear owner.':'Your care workspace.'}</h1><p>Stored workspace · South Africa · SAST (UTC+2)</p></div></div>{notice}
 {view==='Overview'&&<><div className="metrics"><article><span>Active people</span><strong>{data.people.filter(p=>p.active).length}</strong></article><article><span>Enrolled devices</span><strong>{data.devices.filter(d=>d.enabled).length}</strong><small>Enrolment does not confirm connectivity</small></article><article><span>Open tasks</span><strong>{data.tasks.filter(t=>t.stage!=='Completed').length}</strong></article></div><section className="panel"><h2>Latest automatic readings</h2>{readingTable(data.readings.slice(0,10))}</section><section className="panel context"><h2>Set up your connected-care workflow</h2><p>Create people, register an integration, then enrol devices using the identifiers your connector sends.</p><div className="actions"><button className="secondary" onClick={()=>go('People')}>Manage people</button><button className="secondary" onClick={()=>go('Workspace')}>Manage integrations</button><button className="secondary" onClick={()=>go('Devices')}>Enrol devices</button></div></section></>}
 {view==='People'&&<><section className="panel"><div className="filters"><label className="field">Find a person<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Name or town"/></label></div>{data.people.filter(p=>`${p.name} ${p.town}`.toLowerCase().includes(query.toLowerCase())).map(p=><article className="live-row" key={p.id}><div><button className="text-button" onClick={()=>setPersonId(p.id)}>{p.name}</button><p>{p.town} · {p.active?'Monitoring consent recorded':'Monitoring paused'}</p></div>{admin&&<button disabled={busy} className="secondary" onClick={()=>mutate({action:'person-status',id:p.id,active:!p.active})}>{p.active?'Pause monitoring':'Resume monitoring'}</button>}</article>)}{!data.people.length&&<p>No people enrolled yet.</p>}</section>{personId&&<section className="panel"><h2>{personName(personId)} · Readings</h2>{readingTable(readings)}</section>}{admin&&<section className="panel"><h2>Enrol a person</h2><Form busy={busy} label="Save person" onSubmit={f=>mutate({...f,action:'person',consent:f.consent==='on'})}><Input name="name" label="Full name"/><Input name="town" label="Town"/><label className="check-field"><input name="consent" type="checkbox" required/> Monitoring consent has been obtained and documented.</label></Form></section>}</>}
 {view==='Devices'&&<><div className="device-grid">{data.devices.map(d=>{const last=data.readings.find(r=>r.device_id===d.id);return <article className="panel" key={d.id}><Radio/><h2>{d.label}</h2><p>{personName(d.person_id)}</p><dl><div><dt>Identifier</dt><dd>{d.id}</dd></div><div><dt>Measurement</dt><dd>{d.kind}</dd></div><div><dt>Adapter</dt><dd>{d.adapter_id}</dd></div><div><dt>Ingestion</dt><dd>{d.enabled?'Enabled':'Paused'}</dd></div><div><dt>Latest received measurement</dt><dd>{last?date(last.measured_at):'Awaiting first reading'}</dd></div></dl>{admin&&<button className="secondary" disabled={busy} onClick={()=>mutate({action:'device-status',id:d.id,enabled:!d.enabled})}>{d.enabled?'Pause device':'Enable device'}</button>}</article>;})}</div>{!data.devices.length&&<section className="panel"><h2>No devices enrolled</h2><p>First add a person and an integration, then register the device below.</p></section>}{admin&&<section className="panel"><h2>Enrol a device</h2><p>Use the device identifier reported by the connector. Assignments are fixed to preserve reading history.</p><Form busy={busy} label="Enrol device" onSubmit={f=>mutate({...f,action:'device'})}><Input name="id" label="Connector device ID"/><Input name="label" label="Device label"/>{personSelect}<label className="field">Integration<select name="adapterId" required><option value="">Choose an integration</option>{data.integrations.filter(i=>i.enabled).map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</select></label><label className="field">Measurement<select name="kind"><option value="blood-pressure">Blood pressure</option><option value="glucose">Glucose / CGM</option><option value="spo2">SpO2</option></select></label></Form></section>}</>}
 {view==='Care plans'&&<LiveCarePlans plans={data.plans} reviews={data.planReviews} links={data.planTasks} people={data.people} users={data.users} busy={busy} mutate={mutate} openTasks={()=>go('Tasks')}/>}
 {view==='Tasks'&&<><section className="panel"><h2>Care-team follow-ups</h2>{data.tasks.length?data.tasks.map(t=><article className="live-row" key={t.id}><div><h3>{t.title}</h3><p>{personName(t.person_id)}</p><span className="badge">{t.stage}</span></div><form className="task-edit" onSubmit={e=>{e.preventDefault();mutate({...Object.fromEntries(new FormData(e.currentTarget)),action:'task-update',id:t.id});}}><label className="field">Owner<select name="owner" defaultValue={t.owner||''} key={`owner-${t.owner}`}><option value="">Unassigned</option>{data.users.filter(u=>u.active).map(u=><option value={u.id} key={u.id}>{u.email}</option>)}</select></label><label className="field">Stage<select name="stage" defaultValue={t.stage} key={`stage-${t.stage}`}><option>To do</option><option>In progress</option><option>Completed</option></select></label><button className="secondary" disabled={busy}>Save task</button></form></article>):<p>No follow-up tasks yet.</p>}</section><section className="panel"><h2>Create a follow-up</h2><Form busy={busy} label="Create task" onSubmit={f=>mutate({...f,action:'task'})}>{personSelect}<Input name="title" label="What needs to happen?"/></Form></section></>}
 {view==='Reports'&&<section className="panel"><h2>Automatic reading history</h2><p>Latest 500 stored readings, ordered by measurement time. Exported files contain personal information; share them only with authorised recipients.</p><label className="field">Filter by person<select value={personId} onChange={e=>setPersonId(e.target.value)}><option value="">All people</option>{data.people.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><button className="secondary" disabled={!readings.length} onClick={download}>Download readings CSV</button>{readingTable(readings)}</section>}
 {view==='Workspace'&&<><section className="panel"><h2>Automatic integrations</h2><p>BP, glucose / CGM and SpO2 across supported device brands. Registration creates credentials; a compatible gateway or cloud connector must still be configured.</p>{secret&&<div className="credential"><h3>Save this integration credential now</h3><p>Shown only in this session. Treat it like a password.</p><label className="field">Bearer token<input readOnly value={secret.secret}/></label><p>Adapter: {secret.adapterId}</p><button className="secondary" onClick={()=>setSecret(null)}>I have saved it · dismiss</button></div>}{data.integrations.map(i=><article className="live-row" key={i.id}><div><h3>{i.name}</h3><p>{i.id} · {i.enabled?'Enabled':'Paused'}</p></div>{admin&&<div className="actions"><button className="secondary" disabled={busy} onClick={()=>mutate({action:'integration-update',id:i.id,enabled:!i.enabled})}>{i.enabled?'Pause':'Enable'}</button><button className="secondary" disabled={busy} onClick={()=>{if(window.confirm('Replace this credential? The current connector will stop sending until updated.'))mutate({action:'integration-update',id:i.id,rotate:true});}}>Rotate credential</button></div>}</article>)}{admin&&<Form busy={busy} label="Register integration" onSubmit={f=>mutate({...f,action:'integration'})}><Input name="name" label="Integration name"/><Input name="id" label="Adapter ID · e.g. clinic-gateway-adapter"/></Form>}<details><summary>Connector instructions</summary><p>Send JSON to <code>POST /api/readings/</code> with <code>Authorization: Bearer YOUR_TOKEN</code> and <code>X-CareGrid-Adapter: YOUR_ADAPTER_ID</code>. The source must match that adapter ID. Use an enrolled device ID and a unique event ID. Readings before enrolment are rejected.</p><pre>{JSON.stringify({source:'clinic-gateway-adapter',sourceEventId:'unique-event-id',deviceId:'enrolled-device-id',measuredAt:'ISO timestamp with timezone',measurement:{kind:'spo2',value:98,unit:'%'}},null,2)}</pre></details></section>{admin&&<><section className="panel"><h2>Team access</h2>{recoveryLink&&<div className="credential"><h3>One-time recovery link</h3><p>Expires in 30 minutes. Share through your approved secure channel. No email has been sent.</p><label className="field">Recovery link<input readOnly value={recoveryLink}/></label><button className="secondary" onClick={()=>setRecoveryLink('')}>Dismiss recovery link</button></div>}{data.users.map(u=><div className="live-row" key={u.id}><p>{u.email} · {u.role} · {u.active?"Active":"Disabled"}</p>{u.id!==data.user.id&&<button className="secondary" disabled={busy} onClick={()=>mutate({action:"user-status",id:u.id,active:!u.active})}>{u.active?"Disable access":"Enable access"}</button>}</div>)}<Form busy={busy} label="Create team account" onSubmit={f=>mutate({...f,action:'user'})}><Input name="email" label="Email" type="email"/><Input name="password" label="Initial password · at least 14 characters" type="password"/><label className="field">Role<select name="role"><option value="reviewer">Care reviewer</option><option value="admin">Administrator</option></select></label></Form><details><summary>Recover a team member’s account</summary><Form busy={busy} label="Create recovery link" onSubmit={f=>mutate({...f,action:"recovery"})}><label className="field">Account<select name="id" required><option value="">Choose an account</option>{data.users.filter(u=>u.active&&u.id!==data.user.id).map(u=><option key={u.id} value={u.id}>{u.email}</option>)}</select></label><Input name="currentPassword" label="Your administrator password" type="password"/></Form></details><p>No invitation email is sent. Share access details through your approved secure channel.</p></section><section className="panel"><h2>Audit history</h2><p>Latest 100 workspace events.</p><div className="table-scroll"><table><thead><tr><th>Time · SAST</th><th>Action</th><th>Actor</th></tr></thead><tbody>{data.audit.map(a=><tr key={a.id}><td>{date(a.at)}</td><td>{a.action}</td><td>{data.users.find(u=>u.id===a.actor)?.email||a.actor}</td></tr>)}</tbody></table></div></section></>}<section className="panel"><h2>Change your password</h2><Form busy={busy} label="Change password and sign out" onSubmit={f=>mutate({...f,action:"password"})}><Input name="currentPassword" label="Current password" type="password"/><Input name="password" label="New password" type="password"/></Form></section><section className="panel"><h2>Service boundaries</h2><p>CareGrid records device readings and care-team tasks. Clinical alert thresholds, patient messaging and emergency response are not configured. Device status reports ingestion settings and recorded timestamps; it does not confirm that a device is currently online.</p></section></>}
 <footer className="footer"><span>CareGrid by HomeClinicStore</span><span>Automatic readings · People first.</span></footer></main></div></div>;
}
